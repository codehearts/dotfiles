:colorscheme sweets-nejsan

" Only display the tab bar
:set gui=none,tabs

" Enable animations
:set ani

" Disable plugin loading
:set nolpl

" Use ctrl-a to select all in insert mode instead of moving to the beginning of the line
:inoremap <C-a> <Ins><C-a><Ins>
